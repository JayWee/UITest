<Page
    x:Class="UITest.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UITest"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="SliderHeader" Text="LED-Test" Width="300" Height="50" Margin="20" TextAlignment="Center" FontSize="40" FontWeight="Bold"/>
            <Slider x:Name="LEDSlider"  Width="500" StepFrequency="1" Maximum="12" Minimum="1"/>
            <TextBlock x:Name="LEDNumber" Text="1" TextAlignment="Center" FontSize="25" FontWeight="SemiBold"/>
        </StackPanel>

    </Grid>
</Page>
